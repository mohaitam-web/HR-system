// this file contains various constants used throughout the application

// enum for button colors
export enum colorsStatus {
  PRIMARY = "primary",
  SECONDARY = "secondary",
  NONE = "none",
}

// enum for routes Type
export enum routeStatus {
  AUTH = "auth",
  PROTECTED = "protected",
}

// this array is used in quick actions component
export const actions: string[] = [
  "Apply for Leave",
  "KPI Goals",
  "Take Appraisal",
  "View Payslip",
  "Update Profile",
  "Events",
];
