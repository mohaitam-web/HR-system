import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#102A75",
    },
    secondary: {
      main: "#FFC20E",
    },
    secondaryText: {
      main: "#686565ff",
    }
  }, 
});

export default theme;
